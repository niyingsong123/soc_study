# SoC 学习项目协作约定

- 本项目用于学习各模块职责、模块间联系及整个 SoC 架构，默认使用中文并保留英文术语。
- 先读 `README.md`、`module-map.md`、`project-context.md`，再按主题阅读模块入口和 `sources.md`。
- 模块名称使用 **GRBM**。
- 只建立顶层学习目录；已归入父模块的专属子模块先写在父目录文档中，不预建芯片子模块目录；按用户后续要求，各模块建立 `sources/` 资料子目录，每份来源一篇详细笔记，由 `sources/README.md` 索引。用户明确允许 UTCL1 这类公共模块独立建目录，即使某个实例位于 TBE 内部；公共模块入口与实例归属同时记录。目录分组不等于 RTL 实例层级。
- 新模块按用户后续提供的资料补充。当前 `GC` 仅作为已列出的 GL2、GRBM、RLC 的上级归档入口，不扩展新的研究范围。
- `D:\project\no_preject\sdma_repo` 是独立项目，仅允许只读参考；不得修改、移动或重命名其中的内容。本项目 `SDMA/` 只维护外部入口和 SoC 层面的联系，不复制其原始资料或维护另一套详细规格。
- 明确区分用户说明、本地资料摘要、公开架构参考、分析推断与待确认事项；公开 AMD 架构不能直接作为 shaobo/anshi 的设计结论。
- CF 当前按 shaobo 资料中的 Command Fabric 理解；其他代际须重新确认。不要把 CF、SMN 或 CANE 自动当作同一网络。
- 包含关系、接口连接、管理关系和学习分组分别标注；UTCL1/UTCL2 的一级/二级不表示包含，UMC 与 HBM 的连接不表示包含。
- 新资料保留原名、版本和内容，先在 `sources.md` 记录，再更新对应入口、模块映射与待确认事项。没有资料支持的缩写不强行展开。
- `original_file/` 中的文件仅供本地查看，不得上传 GitHub（包括私有仓库）或其他云端服务。该目录必须保持 Git 忽略；不得通过复制、重命名或打包绕过此限制。

- SOC 项目只关联 `D:\project\no_preject\soc_repo`；独立 `sdma_repo` 已取消附加关联。保留本仓库 `SDMA/` 的只读入口，不将外部项目文件纳入同步。
- C01–C05 的五份 Markdown 正文目前缺失；用户已明确选择只同步现有文件，不自行补回。`conversion-report.md` 与 `source-manifest.json` 是历史记录，不证明当前正文存在或当前全文校验通过。
- 用户于 2026-09-25 要求删除 GitHub 上的 `HUBS/assets/`、`UTCL2/assets/` 并清理其 Git 历史；这两个路径必须保持 Git 忽略，不得重新提交或用旧历史合并恢复。既有阅读记录不表示原图仍可从 GitHub 获取。
- 项目目标、约定、资料可用性或待确认事项变化时，同步更新 `project-context.md` 及相关入口；缺失文件使用状态说明，避免保留指向缺失正文的可点击链接。

## 多轮论文研究的接续规则

- 开始研究规划或论文撰写前，阅读 [chip-study-plan.md](chip-study-plan.md) 的已确认范本及 `project-context.md` 的当前项目安排；`sources.md` 的 U9–U24 记录用户要求。
- 通用方法在范本中集中维护，包括微架构主线、行业术语检索、随阅读维护资料集、主题旁资料链接、可变轮次、按数据／请求上下游逐模块执行和完成条件。项目上下文与模块方案引用它，避免重复维护整套方法。
- 当前以 AMD 模块名称组织，先规划、后由本地 Codex 展开细节。按代表性场景中有依据的数据／请求路径，先上游、后下游；控制与管理主题按事件及依赖安排。SWITCH 复核位置结合路径依赖确定，保留已完成两轮的事实，并继续遵守现有 GC/HUBS 分组、SDMA 外部项目及原件管理边界。
- 以 `sources.md` 为全局资料入口，模块 `sources/README.md` 为逐篇索引；每份新旧来源维护一篇详细主笔记，索引说明讲什么及何时值得读。接续先看整体微架构与问题，再读索引/相关笔记，按需回原文。跨模块仅链接复用，不复制详细笔记。
- 模块上下文必须能找到资料集；目前使用各模块 README 的“资料集与接续”小节记录入口、范围和阅读顺序。开始或继续模块任务时先查看该入口；导读及阅读状态留在模块索引，机制细节留在主笔记；资料集位置变化时同步更新入口和总索引。编写模块规划时，同时简记代表性场景、上下游接口与前置结论，已有说明直接链接。

- 具体模块方案已在 [research-roadmap.md](research-roadmap.md) 汇总；新任务同时读取当前模块的 research-plan.md。资料任务尚未全部完成，先查 [source-reading-audit.md](source-reading-audit.md) 的补读与深度缺口；用户明确开始正式论文时，默认下一项为 GC 第 1 轮，接续 SWITCH 时用其修订后的第 3 轮。18 个模块方案和 SDMA 接口方案的完成状态不能当作详细论文轮次完成。相互引用先借用最小接口契约，避免等待双方全部论文的循环前置。

- U23 明确 HBM 以典型接口行为及 PHY、CS、UMC 的微架构运用为重点。先读 [HBM 接口研究范围](project-context.md#hbm-接口研究范围)；厂商器件数据表、料号级电气/时序数值和内部 ECC 等按需参考，不作为必补项或论文启动条件。保持逐篇实际阅读状态，不把范围缩减记成精读完成。CS 仍归档 DF，相关入口与方案已同步。

- U24 将 HBM 的 bank 等资源划分、地址 interleave、跨模块目标选择及返回关联确定为核心必做专题。先读 [请求地址到 HBM bank 的研究方案](HBM/address-interleaving-plan.md)，相关模块各轮持续补齐路径与统一地址例子；不再等全部模块论文结束后才考虑该联系。区分缓存分片、翻译、内存目标交织及 DRAM 地址解码，不能预设所有请求串经同一条模块链。U23 的可选范围仅针对厂商器件专属细节，不削弱上述机制深度。

## 资料笔记的证据规则

- 用户要求 U19–U21 已明确授权模块资料子目录与逐篇详细笔记；旧“只在总索引简写、不逐篇建笔记”的方法已由范本 v1.3 替代。
- 笔记记录来源性质、版本、实际阅读章节/函数、机制和重要条件、完成语义与可复用问题；全文未得或仅摘要已读必须显式标注，不能伪造精读。
- C01–C05 本次依据已提交页图整理参考设计笔记，并核看关键图、核算 C04 地址例子；不恢复缺失转换正文，不读取/上传原件。带 HYGON 标识的页图不称为 AMD 官方资料，目标对应仍待核实。
- R9 是 CHI 模型指南、R12 是架构概述、R23 是正式 CHI E.a 规范选读；模型、产品介绍、标准和驱动不能混作同一级证据。驱动 no-op、TODO、条件跳过和模拟器简化应保留。
- 当前为 103 篇来源笔记和 19 个模块索引；99 篇有不同范围的阅读记录，其中 MEM11/MEM16 仅规范转录选读、原版图表待核；IO4 与 L1–L3 共 4 篇无本次可读正文。R13 已补正文、C01 第 1/4 页已补、R23 已提供正式 CHI E.a 选读。具体关闭与受限项见 [source-reading-audit.md](source-reading-audit.md)，不得将数量当作深度完成率或论文轮次进度。

- RSMU 的 remote SMU 全称已有 AMD 作者原始提交依据（MG5），不再视为仅凭字母猜测；目标实例、内部结构与 SMU 层级仍待证据。C01 的 BOWEN 图是参考拓扑，不是 AMD 目标规格。
- 资料补齐按范本 v1.4 区分读取失败、无文字层、转录/历史版本与正式原版。先复用已补笔记，只对实际剩余项继续回原文；本地补 L1–L3 时仍只读、只回写 SoC 接口结论。

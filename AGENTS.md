# SoC 学习项目协作约定

- 本项目用于学习各模块职责、模块间联系及整个 SoC 架构，默认使用中文并保留英文术语。
- 先读 `README.md`、`module-map.md`、`project-context.md`，再按主题阅读模块入口和 `sources.md`。
- 模块名称使用 **GRBM**。
- 只建立顶层学习目录；已归入父模块的专属子模块先写在父目录文档中，不预建子目录。用户明确允许 UTCL1 这类公共模块独立建目录，即使某个实例位于 TBE 内部；公共模块入口与实例归属同时记录。目录分组不等于 RTL 实例层级。
- 新模块按用户后续提供的资料补充。当前 `GC` 仅作为已列出的 GL2、GRBM、RLC 的上级归档入口，不扩展新的研究范围。
- `D:\project\no_preject\sdma_repo` 是独立项目，仅允许只读参考；不得修改、移动或重命名其中的内容。本项目 `SDMA/` 只维护外部入口和 SoC 层面的联系，不复制其原始资料或维护另一套详细规格。
- 明确区分用户说明、本地资料摘要、公开架构参考、分析推断与待确认事项；公开 AMD 架构不能直接作为 shaobo/anshi 的设计结论。
- CF 当前按 shaobo 资料中的 Command Fabric 理解；其他代际须重新确认。不要把 CF、SMN 或 CANE 自动当作同一网络。
- 包含关系、接口连接、管理关系和学习分组分别标注；UTCL1/UTCL2 的一级/二级不表示包含，UMC 与 HBM 的连接不表示包含。
- 新资料保留原名、版本和内容，先在 `sources.md` 记录，再更新对应入口、模块映射与待确认事项。没有资料支持的缩写不强行展开。
- `original_file/` 中的文件仅供本地查看，不得上传 GitHub（包括私有仓库）或其他云端服务。该目录必须保持 Git 忽略；不得通过复制、重命名或打包绕过此限制。

- SOC 项目只关联 `D:\project\no_preject\soc_repo`；独立 `sdma_repo` 已取消附加关联。保留本仓库 `SDMA/` 的只读入口，不将外部项目文件纳入同步。
- C01–C05 的五份 Markdown 正文目前缺失；用户已明确选择只同步现有文件，不自行补回。`conversion-report.md` 与 `source-manifest.json` 是历史记录，不证明当前正文存在或当前全文校验通过。
- 项目目标、约定、资料可用性或待确认事项变化时，同步更新 `project-context.md` 及相关入口；缺失文件使用状态说明，避免保留指向缺失正文的可点击链接。

## 多轮论文研究的接续规则

- 本项目主要研究 AMD 芯片，按用户给出的 AMD 模块名称组织最终方案和论文。开始前阅读 `project-context.md` 的“AMD 命名与微架构研究规划”及 `sources.md` 的 U9–U11。
- 先核对模块是否有行业通用名称、别名、历史名称或功能相近的实现，再联合检索。建立带来源、代际/版本和适用边界的术语对照；区分别名、上位类别、子功能与类比，不能仅因名字不同遗漏通用研究，也不能把相关术语直接视为完全等价。UTCL2 联合 MMU 检索是研究方向，不是目标实现等同的结论。
- 以整体微架构为主线：先建结构与典型事务流程，再划分子模块、映射 feature 和编排轮次。每个主题须说明架构归属、接口/资源依赖及对事务路径的影响；每轮把研究结果整合回同一总图与主文档。
- 当前先完成逐模块研究规划，轮数按需增减；完整细节留给后续本地 Codex 执行。优先采用高质量一手资料，区分公开事实、推导、参考设计与目标芯片待确认事项。
- SWITCH 实际完成两轮；原六轮规划可重新评估，规划修改不计为完成第三轮。逐模块完成方案后，再评估跨模块专题；保持现有 GC、SDMA 和原件管理边界。
